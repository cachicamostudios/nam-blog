---
const { title, title_en, subtitle, subtitle_en, pubDate, author } = Astro.props;
---

<section class="pt-16 pb-24 px-6 md:px-0">
  <div class="max-w-3xl mx-auto">
    <h1 class="text-4xl font-bold mb-2">
      <span class="lang-es">{title}</span>
      <span class="lang-en hidden">{title_en ?? title}</span>
    </h1>
    <h2 class="text-xl text-neutral-600 dark:text-neutral-400 mb-4">
      <span class="lang-es">{subtitle}</span>
      <span class="lang-en hidden">{subtitle_en ?? subtitle}</span>
    </h2>
    <p class="text-sm text-neutral-400 mb-10">
      🗓️ {new Date(pubDate).toLocaleDateString('es-ES')} · ✍️ {author}
    </p>
    <slot />
  </div>
</section>
